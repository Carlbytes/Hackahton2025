using UnityEngine;

// FILE: AsteroidData.cs
public class AsteroidData : MonoBehaviour
{
    // This attribute tells the Unity Inspector to not try and draw this complex object.
    [HideInInspector]
    public NearEarthObject neoData;
}